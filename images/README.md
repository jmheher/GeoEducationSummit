# images/

Drop the top-of-card images here and reference them from `sites.js`, e.g.
`image: "images/catchment-summary.png"`.

Suggested size: 320x180 or larger, 16:9 aspect ratio (cards crop to fill).
Missing or broken images fall back to a "no image" placeholder automatically.
